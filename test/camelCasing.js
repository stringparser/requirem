'use strict';

var camelCase = require('camelcase');
var should = require('should');

module.exports = function patterns(requirem){

  it('should have camelCased keys', function(){
    var dir = requirem('./modules/');
    Object.keys
  });
};
